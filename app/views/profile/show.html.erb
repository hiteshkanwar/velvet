<% # If user navigates to own profile %>
<% # Display self %>

<%if @user.username == session[:username] %>
    <%= render :partial => 'own' %>
<% else %>
    <%= render :partial => 'other' %>
<% end %>